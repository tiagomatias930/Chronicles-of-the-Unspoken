import { GameLevel } from '../types';

export class SoundManager {
  constructor() {
    // Audio logic removed per user request.
  }

  public toggleMute(muted: boolean) {
     // No-op
  }

  public async playAmbience(level: GameLevel) {
     // No-op
  }
}